import { ComplexScene } from "./ComplexScene"

export class ComplexScene2D extends ComplexScene {
    public constructor(name : string, author : string, psScale=1.0, landscapeModeFlag = false) {
        super(name, author);
    }
}